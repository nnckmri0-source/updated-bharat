# Live fallback for content published from the admin panel after the last build.
# Static pages (built slugs) take priority, so only slugs WITHOUT a static page
# hit these rewrites and render live from Firebase.
/news/* /news/live.html 200
/channel/* /channel/live.html 200
